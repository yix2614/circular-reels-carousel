
export interface VideoItem {
  id: string;
  videoUrl: string;
  username: string;
  description: string;
}

export interface CarouselConfig {
  width: number;
  height: number;
  maxMobileWidth: number;
}
